# Base apt tools

sudo apt-get install build-essential
sudo apt-get install git
sudo apt-get install curl
sudo apt-get install wget

# Ghostty

Install .deb from https://ghostty.org/docs/install/binary#ubuntu

# Zsh and Oh My Zsh

sudo apt-get install zsh
chsh -s $(which zsh)

Log out and back in
verify with
zsh --version
echo $SHELL

# Chrome
install from the web

# Directories
create $HOME/repos/site
create $HOME/repos/work
create $HOME/repos/personal

# Github

ssh-keygen -t ed25519
log into github.com
add key
clone dotfiles
























# Fixing the dock
